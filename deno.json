{
  "tasks": {
    "dev": "deno run -A npm:vite",
    "build": "deno run -A npm:vite build",
    "preview": "deno run -A npm:vite preview",
    "typecheck": "deno run -A npm:tsc --noEmit",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "test": "deno test"
  },
  "lint": {
    "exclude": ["dist", "node_modules"]
  },
  "fmt": {
    "exclude": ["dist", "node_modules"]
  }
}
